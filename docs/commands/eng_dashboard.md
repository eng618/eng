## eng dashboard

Launch the interactive Project & Git Dashboard

### Synopsis

Opens a full-screen "mission control" interface to view all configured projects.
It displays your projects in a list, and shows the live status of their repositories
(cloned state, current branch, and uncommitted changes) in real-time.

Press ? inside for all shortcuts. Quick keys: Tab switch panes, 1/2 switch tabs,
/ filter projects, r refresh, a add, q quit.

```
eng dashboard [flags]
```

### Examples

```
  eng dashboard
```

### Options

```
  -h, --help   help for dashboard
```

### Options inherited from parent commands

```
      --config string   config file (default is $HOME/.eng.yaml)
  -v, --verbose         verbose output
```

### SEE ALSO

* [eng](eng.md)	 - A personal CLI to facilitate workflow and system maintenance.

