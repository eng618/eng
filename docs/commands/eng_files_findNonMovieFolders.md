## eng files findNonMovieFolders

Find and optionally delete non-movie folders

### Synopsis

This command searches recursively through the supplied directory for directories
that do not contain video files (mp4, mkv, avi, mov, wmv, flv, webm, mpeg, mpg, m4v).
It identifies top-level subdirectories within the supplied directory that lack
any such files anywhere within their structure.

It calculates folder disk space, lists folder contents, and prompts for confirmation before deletion.

```
eng files findNonMovieFolders [directory] [flags]
```

### Options

```
      --dry-run   Perform a dry run without deleting folders. Set to false to enable deletion. (default true)
  -h, --help      help for findNonMovieFolders
```

### Options inherited from parent commands

```
      --config string   config file (default is $HOME/.eng.yaml)
  -v, --verbose         verbose output
```

### SEE ALSO

* [eng files](eng_files.md)	 - A command for managing files

###### Auto generated by spf13/cobra on 30-Jul-2026
