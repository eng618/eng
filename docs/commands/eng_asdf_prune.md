## eng asdf prune

Prune outdated asdf tool versions not listed in .tool-versions

### Synopsis

Reads the root (user level) .tool-versions file and scans project directories for additional .tool-versions files, then removes installed tool versions that are not explicitly defined.

Use the --interactive (-i) flag to present a multi-select prompt for choosing specific versions to uninstall.
Use the --dry-run (-d) flag to preview which versions would be uninstalled.
Use the --no-scan flag to disable searching development directories for project .tool-versions files.

```
eng asdf prune [flags]
```

### Options

```
  -c, --config string     Path to specific .tool-versions file (default scans $HOME and dev folders)
  -d, --dry-run           Show what versions would be deleted without removing them
  -f, --force             Skip confirmation prompt in default mode
  -h, --help              help for prune
  -i, --interactive       Interactively select installed tool versions to delete
      --no-scan           Disable scanning development directories for project .tool-versions files
  -p, --plugin string     Limit cleanup to a specific plugin (e.g. nodejs)
      --scan-dir string   Additional directory to recursively scan for .tool-versions files
```

### Options inherited from parent commands

```
  -v, --verbose   verbose output
```

### SEE ALSO

* [eng asdf](eng_asdf.md)	 - Manage asdf version manager plugins and installs

###### Auto generated by spf13/cobra on 30-Jul-2026
