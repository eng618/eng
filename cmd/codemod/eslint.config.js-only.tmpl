import { base, prettier } from '@gv-tech/eslint-config';

/**
 * ESLint configuration for JavaScript projects.
 * Uses @gv-tech/eslint-config for sensible defaults.
 */
export default [
  ...base,
  ...prettier,
  // Add project-specific overrides here
  {
    ignores: ['eslint.config.mjs'],
  },
];
