# Documentation at https://goreleaser.com

# The lines below are called `modelines`. See `:help modeline`
# Feel free to remove those if you don't want/need to use them.
# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

project_name: eng

before:
  hooks:
    # You may remove this if you don't use go modules.
    - go mod tidy
    # you may remove this if you don't need go generate
    # - go generate ./...

builds:
  - # Path to main package or binary
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    # Inject version info using ldflags and Goreleaser's template variables
    ldflags:
      - -s -w # Strip symbols and debug info
      - -X github.com/eng618/eng/cmd/version.Version={{.Version}}
      - -X github.com/eng618/eng/cmd/version.Commit={{.ShortCommit}} # Use ShortCommit for brevity
      - -X github.com/eng618/eng/cmd/version.Date={{.Date}} # Use GoReleaser's build date
      - -X github.com/eng618/eng/internal/config.DefaultClientID={{.Env.OPENPANEL_CLIENT_ID}}
      - -X github.com/eng618/eng/internal/config.DefaultClientSecret={{.Env.OPENPANEL_CLIENT_SECRET}}


archives:
  - formats:
      - tar.gz
    # this name template makes the OS and Arch compatible with the results of `uname`.
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
    format_overrides:
      - goos: windows
        formats: zip
checksum:
  name_template: "checksums.txt"
snapshot:
  # Use commit hash for snapshot version template for clarity
  version_template: "{{ .ShortCommit }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "(?i)skip ci" # Exclude common skip patterns
      - '^chore\(release\):' # Exclude goreleaser's own release commits
      - '^chore\(CHANGELOG\):' # Exclude changelog update commits

homebrew_casks:
  - name: eng
    repository:
      owner: eng618
      name: homebrew-eng
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: Eric N. Garcia
      email: eng618@garciaericn.com
    commit_msg_template: "Update eng to {{ .Tag }}"
    homepage: "https://github.com/eng618/eng"
    description: "Personal CLI to help facilitate my normal workflow"
    license: "MIT"
    url:
      verified: "github.com/eng618/eng"
    binaries:
      - eng
    generate_completions_from_executable:
      args:
        - completion
      base_name: eng
      shell_parameter_format: cobra
      shells:
        - bash
        - zsh
        - fish
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/eng"]
          end
