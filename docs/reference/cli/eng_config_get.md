## eng config get

Print a single configuration value and where it comes from

### Synopsis

Prints one configuration value with its source (env, file, or default).

Example: eng config get git.dev_path

```
eng config get <key> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --config string   config file (default is $HOME/.eng.yaml)
      --no-color        disable colored output (also respects NO_COLOR)
  -v, --verbose         verbose output
```

### SEE ALSO

* [eng config](eng_config.md)	 - Manage the cli's config file.

