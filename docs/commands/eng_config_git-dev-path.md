## eng config git-dev-path

Update config git dev path

### Synopsis

This command sets the path to the development folder containing git repositories in the config file.

```
eng config git-dev-path [flags]
```

### Options

```
  -h, --help   help for git-dev-path
```

### Options inherited from parent commands

```
      --config string   config file (default is $HOME/.eng.yaml)
  -v, --verbose         verbose output
```

### SEE ALSO

* [eng config](eng_config.md)	 - Manage the cli's config file.

