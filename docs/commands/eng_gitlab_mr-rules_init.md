## eng gitlab mr-rules init

Interactively generate a MR rules JSON file

```
eng gitlab mr-rules init [flags]
```

### Options

```
      --force           Overwrite existing file without prompt
  -h, --help            help for init
      --output string   Output path for rules JSON (default: gitlab-rules.json)
      --yes             Use defaults without prompting
```

### Options inherited from parent commands

```
      --config string   config file (default is $HOME/.eng.yaml)
  -v, --verbose         verbose output
```

### SEE ALSO

* [eng gitlab mr-rules](eng_gitlab_mr-rules.md)	 - Manage GitLab merge request rules

