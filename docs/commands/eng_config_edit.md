## eng config edit

Interactively edit the eng CLI configuration

### Synopsis

Launch a beautiful TUI wizard to easily update your .eng.yaml configuration settings.

By default, this command launches the interactive editor. Use --interactive=false if you want to bypass the wizard in the future (though currently, the interactive editor is the primary function of this command).

```
eng config edit [flags]
```

### Options

```
  -h, --help          help for edit
  -i, --interactive   Launch the interactive TUI editor (default true)
```

### Options inherited from parent commands

```
      --config string   config file (default is $HOME/.eng.yaml)
  -v, --verbose         verbose output
```

### SEE ALSO

* [eng config](eng_config.md)	 - Manage the cli's config file.

###### Auto generated by spf13/cobra on 27-Jul-2026
